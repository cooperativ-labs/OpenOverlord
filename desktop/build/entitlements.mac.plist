<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <!--
      Hardened-runtime entitlements required for notarization. No App Sandbox:
      Overlord spawns child processes (the server utilityProcess, agent CLIs, and
      terminal windows via osascript) and reads the user's repositories, all of
      which the sandbox would block. Distribute via Developer ID + notarization.
    -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
    <!-- Open Terminal/iTerm via osascript for the agent launch path (TCC prompts once). -->
    <key>com.apple.security.automation.apple-events</key>
    <true/>
  </dict>
</plist>
