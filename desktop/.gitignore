# Electron main/preload bundles (regenerated by `yarn workspace @overlord/desktop build`)
dist-electron/
*.tsbuildinfo

# Staged by scripts/build-desktop.ts before electron-builder
server/
webapp-dist/
sqlite/
staging/

# Staged from webapp/public/images/512.png by scripts/build-desktop.ts
build/icon.png

# electron-builder output
release/

# npm artifacts (this package uses Yarn)
package-lock.json
node_modules/
