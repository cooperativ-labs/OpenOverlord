# Overlord Component Interaction Contract

Contract Version: `0.65-draft`

## Purpose

This is the normative specification for how Overlord's components interact. It defines:

- The **component registry** — what each component owns and is responsible for
- **Interaction surfaces** — the only sanctioned communication paths between components
- **Stable interfaces** — what cannot change without a contract version bump
- **Extension points** — the only sanctioned ways to extend Overlord
- **Conformance requirements** — what a shipped component must satisfy before integration
- **Maintenance rules** — when and how to update this document

**Agents and developers MUST read this document before implementing any change that crosses module boundaries, and MUST update it before implementing any change that extends or conflicts with the current contract.**

See `.claude/skills/component-contract.md` for the enforced agent workflow.

## Editions

Overlord ships in two editions that share one contract:

- **Overlord Local** (today's default): the Electron desktop shell supervises a
  loopback backend over SQLite on the user's machine. No account or network
  dependency. This is the behavior all pre-`0.55-draft` contract text describes,
  and it is **unchanged** by the Cloud edition.
- **Overlord Cloud** (additive, `0.55-draft`): the backend and system-of-record
  database run as a hosted control plane; the desktop app, web app, CLI, and one
  or more execution targets (runners) connect to it over HTTPS. Same REST +
  protocol + realtime contract, same desktop/CLI binaries — only the configured
  `backend_url` and auth differ.

Both editions are first-class. A contract change "for Cloud" must never regress
Local; where a surface differs by edition this document calls it out explicitly.

## Contract Version

Current version: `0.66-draft`

The contract version is incremented when any stable interface changes. All conformance manifests must declare the contract version they were validated against.

| Version      | Changes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `0.66-draft` | WS-F6 **mission branch observation writeback** (plan `planning/feature-plans/client-checkout-bridge-unification.md`). **Database Layer:** new `mission_branch_observations` table stores the latest client-reported `deriveBranchStatus` result per `(execution_target_id, mission_id)` (`status`, `dirty`, optional `worktree_path`, `observed_at`). **REST API Layer:** `POST /api/execution-targets/:id/mission-branch-observations` accepts `RecordMissionBranchObservationsBody { observations[] }` from the acting device target; `GET /api/missions/:id` merges cached client branch state into `MissionBranchDto` without running git or checking linked paths on the server. **Web:** local-target branch observation writes back successful desktop/dev-proxy observations and invalidates mission detail. `MissionBranchDto` uses `observedAt` + `observationSource: 'client'` for persisted client truth. Schema migration required. |
| `0.65-draft` | WS-F5 **remote target async mutations** (plan `planning/feature-plans/client-checkout-bridge-unification.md`). **REST API Layer / Runner Layer:** checkout-local mutations for a **non-caller** execution target queue through `execution_requests` with `requested_source: local_target_mutation` and namespaced `metadata_json.overlord.localTargetMutation` (`kind: branch_action \| worktree_purge`, capability + input payload). `POST /api/missions/:id/branch/action` and worktree purge/remove endpoints queue when the project's selected target is not the acting device; `POST /api/runner/requests/:id/completed` records mutation results (`launched` / `failed`). **CLI/Runner:** `ovld runner once\|start` executes queued mutations in-process before agent launches. **Web:** branch panel and worktree purge/remove detect remote target selection, queue via REST, and show pending mutation state from active execution requests + SSE invalidation. `ExecutionRequestDto` gains optional `localTargetMutationKind`; `RemoveWorktreeBody` / `PurgeMergedWorktreesBody` gain remote-queue fields. Additive; no database schema or migration. |
| `0.64-draft` | WS-F4 **client resource observations** (plan `planning/feature-plans/client-checkout-bridge-unification.md`). **Database Layer:** new `target_resource_observations` table stores the latest client-reported `observeResource` result per `(execution_target_id, resource_id)` (`state`, optional `git_root`/`branch`/`git_commit`, `observed_at`). **REST API Layer:** `POST /api/execution-targets/:id/observations` accepts `RecordTargetResourceObservationsBody { observations[] }` from the acting device target (desktop/web dev proxy or runner on claim); `GET /api/projects/:id/resources` merges lifecycle status with the latest matching observation. **Web:** `ProjectRepositoryProvider` periodically observes linked resources on focus/interval and writes results back. **CLI/Runner:** `ovld runner once|start` reports observations after a successful claim. `ProjectResourceDto` gains optional `observedAt` + `observationSource: 'client'`. Schema migration required. |
| `0.63-draft` | WS-F3 **server checkout slimming** (plan `planning/feature-plans/client-checkout-bridge-unification.md`). **REST API Layer:** the webapp backend is a pure control plane for checkout-local work on **all** dialects — including loopback SQLite. `GET …/repository`, mission branch lists, branch actions (unless `clientExecuted`), commit-message drafting (unless `diff` is uploaded), and worktree list/purge endpoints no longer read or mutate linked checkout paths server-side; they return metadata, empty lists, or typed `LOCAL_FILESYSTEM_UNAVAILABLE`. Resource lifecycle DTOs preserve recorded status instead of observing paths on the server. `.overlord/project.json` writes on resource create are client-owned (desktop bridge / CLI). **Dev fallback:** `meta.capabilities.localTarget === 'in_process_server'` and `POST /api/local-target/invoke` are opt-in via `OVERLORD_DEV_IN_PROCESS_LOCAL_TARGET=true` on loopback SQLite only (browser-without-desktop contributor workflows). Clarifies `0.58-draft`: the local SQLite server follows the same hosted checkout rule — no linked-path git/fs via REST. Additive clarification; no database schema, migration, closed vocabulary, or auth impact. |
| `0.62-draft` | WS-F2 **branch & worktree UI via desktop bridge** (plan `planning/feature-plans/client-checkout-bridge-unification.md`). **Desktop Shell / Service Layer:** `deriveBranchStatus` bridge calls now return full branch observations (`status`, `dirty`, `worktreePath`) via shared `observeMissionBranchGit`; `listWorktrees` defaults an empty `worktreeRoot` to the desktop `~/.ovld/worktrees` path. **REST API Layer:** `BranchActionBody` gains optional `clientExecuted` + `summary` so git can run on the client and the server records activity only; `GenerateCommitMessageBody { diff? }` lets the client upload a locally gathered diff for server-side summarization; `RemoveWorktreeBody` gains optional `clientExecuted` + `primaryRepoPath` for hosted acknowledgement after client-side removal. **Web:** mission branch picker, branch panel actions, commit-message drafting, and Settings → Worktrees list/purge flows route through `local-target-client` when a local target is available; `MissionBranchDto` gains optional `observedAt` + `observationSource: 'client' \| 'server'` for client git observations; browser-only Cloud users see an "Open Overlord Desktop" gate and the Worktrees nav item is hidden when `meta.capabilities.localTarget === 'unavailable'`. Additive; no database schema, migration, or auth impact. |
| `0.61-draft` | WS-F1 **unified desktop local-target bridge** (plan `planning/feature-plans/client-checkout-bridge-unification.md`). **Desktop Shell:** `window.overlord` gains `invokeLocalTarget(call)` — a single audited preload dispatch for checkout-local capabilities (`readRepositoryTree`, `listBranches`, `observeResource`, `listWorktrees`, `deriveBranchStatus`, `performBranchAction`, `generateCommitMessageFromLocalDiff`, `writeProjectMetadata`, plus stubbed `readCurrentDiff`) returning the shared `CapabilityResult` envelope; IPC enforces an allowlist of linked checkout roots. The legacy `readRepositoryMentionPaths` preload op is removed (callers use `readRepositoryTree` via the bridge). **REST API Layer:** additive dev-only `POST /api/local-target/invoke` proxies the same capability envelope through the in-process provider on loopback SQLite only; hosted Postgres returns typed `LOCAL_TARGET_REQUIRED`. **Web:** `local-target-client.ts` feature-detects the desktop bridge, falls back to the dev REST proxy when `meta.capabilities.localTarget === 'in_process_server'`, and otherwise fails typed; `@` mentions and `ProjectRepositoryContext` merge repository tree/git fields from the bridge when available. Additive; no database schema, migration, closed vocabulary, or auth impact. |
| `0.60-draft` | WS-F0 **client checkout boundary hardening** (plan `planning/feature-plans/client-checkout-bridge-unification.md`). **REST API Layer:** `GET /api/meta` `capabilities` gains `localTarget: 'in_process_server' \| 'unavailable'`, reporting whether this backend build may still serve checkout-local reads through an in-process dev fallback (`sqlite`) or not (`postgres`). Desktop-hosted clients use the `window.overlord` bridge for checkout work on hosted backends regardless of this flag. **Protocol Layer:** when the CLI targets a non-loopback backend, `discover-project` resolves the working directory from the client filesystem (`.overlord/project.json` walk) before calling the control plane; `--project-id` remains a database-only lookup on the server. **Web:** project-scoped UI (`LiveFileChanges`, `ProjectSettingsSection`) reads repository `rootPath` from `ProjectRepositoryContext` (selected execution target) instead of `useProjectRepository(..., null)`. **Service Layer:** `primaryResource` resolution for branch/worktree helpers is scoped to the project's selected or sole eligible execution target. Additive; no database schema, migration, closed vocabulary, or auth impact. |
| `0.59-draft` | Adds **per-project execution target selection** for queue-here/run-there (WS-C / R3). **REST API Layer:** new `GET/PUT /api/projects/:id/execution-target` (gated `launch:read` / `launch:configure`) returns `ProjectExecutionTargetDto { selectedExecutionTargetId, eligibleTargets[] }` where each `EligibleExecutionTargetDto` carries `executionTargetId`, `type`, `label`, `deviceLabel`, `reachable`, and `primaryResourceConnected`; `UpdateProjectExecutionTargetBody { executionTargetId: string \| null }` persists the acting workspace user's choice in `project_user_preferences.preferences_json.selectedExecutionTargetId` (no new column). Eligible targets are the acting user's active `workspace_user_execution_targets` rows that can reach a primary `project_resource` on that target. Request creation (`POST /api/objectives/:id/launch`) now stamps `execution_requests.execution_target_id` from the stored selection, or the sole eligible target when unambiguous, or `NULL` when multiple targets exist and none is selected. **Service Layer:** `RunnerQueueProvider` stub and `createDefaultLocalTargetRegistry` register the runner-queue transport for reachable non-co-located local targets (capability bodies follow in WS-D). Additive; no database schema, migration, closed vocabulary, protocol, auth, or connector impact (runtime `CONTRACT_VERSION` constant unchanged). |
| `0.58-draft` | Generalizes the **local filesystem ownership** rule for Cloud. **REST/API Backend:** hosted backends — including the loopback SQLite edition — are metadata/control-plane services: they may persist project resources, queue execution requests, serve protocol/realtime state, and receive runner writebacks, but MUST NOT derive linked-checkout availability from the backend host filesystem and MUST NOT run checkout-local Git/worktree operations via REST. Checkout-local reads and mutations run on execution targets through the desktop local-target bridge (or the opt-in `POST /api/local-target/invoke` dev proxy on loopback SQLite). Resource DTOs from the control plane preserve recorded resource lifecycle status instead of marking paths `missing` based on the server filesystem. Repository browsing, branch status enrichment, branch actions, commit-message diff drafting, and worktree management return metadata-only responses or typed `LOCAL_FILESYSTEM_UNAVAILABLE` errors unless the client supplies results (`clientExecuted`, uploaded `diff`) or uses the dev invoke proxy. **CLI/Runner/Desktop:** local execution targets own checkout inspection, `.overlord/project.json`, Git status/diff, branch/worktree preparation, and actual agent file work. No database schema, migration, closed vocabulary, auth, connector, or REST shape impact. |
| `0.57-draft` | Clarifies **local filesystem ownership for Cloud-linked project directories**. **REST API Layer:** `POST /api/projects/:id/resources` and `CreateProjectBody.primaryResource` still record project resources in the backend system of record, but hosted/Postgres backends MUST NOT attempt to write `.overlord/project.json` to the server filesystem for user checkout paths. Local SQLite backends may keep writing the metadata as a same-machine convenience. **CLI Layer:** `ovld create-project` and `ovld add-cwd` write `.overlord/project.json` on the local filesystem after the backend successfully creates the matching resource. **Desktop Shell:** the feature-detected `window.overlord` preload bridge gains `writeProjectMetadata({ directoryPath, projectId, resourceId, isPrimary })` so the desktop-hosted SPA can write local checkout metadata after linking a directory while using a cloud backend. No database schema, migration, closed vocabulary, auth, connector, or REST shape impact. |
| `0.56-draft` | Adds a **per-file change-rationale override** on `deliver` (mission `ove:17`). **Protocol Layer:** `deliver` gains optional `--skip-rationale-for-json` / `--skip-rationale-for-file`, each an array of `{ file_path, reason }` entries (`filePath` alias accepted). Agents use this when delivery would otherwise fail `missing_rationale` for a tracked file they did not change (e.g. concurrent edits in a shared worktree). The backend skips rationale-coverage enforcement for listed paths, persists the skips on the delivery `payload_json.rationaleSkips`, and marks matching `changed_files.observed_metadata_json` with `{ rationaleSkipped: true, skipReason }`. Review coverage treats skipped files as `skipped` rather than `missing_rationale`. **CLI Layer:** at `deliver`, paths listed in `--skip-rationale-for-*` are removed from the auto-injected changed-file delta before it is sent. Additive; no database schema, migration, closed-vocabulary, REST shape, auth, or connector impact (runtime `CONTRACT_VERSION` constant unchanged). |
| `0.55-draft` | Codifies **Overlord Cloud** as an *additive hosted edition* offered alongside the unchanged local-only edition (mission `coo:5`, plan `planning/feature-plans/overlord-cloud-architecture.md`). This entry is the contract change that **precedes** implementation; it specifies the stable surface so later objectives implement against it. Both editions speak the same REST + protocol + realtime contract and ship the **same desktop/CLI binaries** — only the configured `backend_url` and auth differ. **Local-only behavior is explicitly unchanged**: the loopback desktop shell, the embedded server, SQLite, same-origin SPA, and no-account operation all keep working exactly as today. Per-layer surface (additive unless noted): **Database Layer** — `execution_targets.type` (already an open vocabulary) gains the sanctioned values `local`, `cloud_persistent`, `cloud_sandbox`, `ssh`; targets gain nullable, namespaced provider/provisioning metadata (provider, region, provisioned service id, lifecycle/readiness state, storage stats, capability flags) and target heartbeat/status fields (a target-status surface distinct from device-specific `devices.last_seen_at`) so online/idle/busy/stale/disabled can be derived; `project_resources` gains a location model distinguishing local vs provider vs sandbox paths so a resource can be scoped to a specific target. The shared system-of-record may be **hosted Postgres (Neon)**; the live backend runtime must finish adapter selection so the production path no longer opens `better-sqlite3` directly, with conformance for `entity_changes`, realtime/sync, protocol idempotency, execution-request queue claiming, and stale-launch recovery against Postgres. **REST API Layer** — additive provider-neutral target provisioning, heartbeat/status, and capability DTOs for non-local runners; the runner queue endpoints (`/api/runner/*`) remain the **sole claim/launch boundary** and stay centralized in service-layer transactions — provider adapters MUST NOT claim work from database internals. Backend serves REST + protocol + runner-queue + realtime over HTTPS and owns DB credentials; clients never connect to Neon directly. **CLI Layer** — runner registration for non-local targets; a documented **no-persist-config container mode** (consume the injected runtime `OVERLORD_BACKEND_URL` + scoped service token, never write `overlord.toml`, extending `0.36-draft`); target `doctor` checks for missing agent CLIs, Git creds, package managers, and local services; `backend_url`/`backend_mode` already select local vs cloud (`0.10`/`0.11-draft`). **Runner Layer** — target-aware beyond device fingerprint; capability matching on claim; cloud-safe path resolution and branch/worktree defaults; an **outbound-only** wakeup baseline (poll, evolving to long-poll/runner SSE) with **no inbound port to any target, ever**; a **runner service mode** (`ovld runner install-service` / `uninstall-service` / `logs` + a hardened headless `start` loop for `launchd`/`systemd`/containers) with headless agent launch (no GUI terminal/AppleScript), per-request stdout/stderr logs, reliable launch-failure reporting, and optional `tmux` inspection. **Auth Layer** — scoped **service tokens** for cloud/headless targets that can claim only their own target/workspace's requests, with rotation/revocation; Better Auth stays the single interactive auth layer for both editions (Neon Auth is **not** adopted). **Web Layer (webapp)** — the SPA API/realtime client must accept a **runtime-injected API base URL** instead of assuming same-origin; the Vercel web build keeps same-origin/env-based configuration and connects realtime **directly to the Railway backend** (not proxied through Vercel functions); any DB-touching server code is pinned to the Node runtime + `iad1`. **Desktop Shell** — a documented **remote-client mode** added alongside the unchanged local loopback mode: persisted backend **profiles** (`{ id, label, mode: 'local' | 'remote', backendUrl }` + active id) in `userData/settings.json`; in remote mode the bundled SPA still loads from a local origin but its API base points at the hosted backend and the **embedded server is not forked**; per-profile Electron **session partitions**; **bearer-token** login persisted per-backend via `safeStorage`/keychain and injected into the SPA client and any spawned `ovld` process; CSP `connect-src` and the navigation guard become functions of the active profile (adding the remote REST/`wss://` origins while keeping navigation shell-local); HTTPS `/api/health` checks with a Local fallback; and a reload-and-login switcher UI / first-run chooser. New preload bridge ops (`getActiveBackend`, `listBackends`, `addBackend`, `removeBackend`, `switchBackend`). **Connector Layer** — unchanged surface; connectors remain `backend_url`-agnostic and keep working against either edition. **Protocol Layer** — unchanged command/envelope surface; protocol writeback (`attach → update/heartbeat → ask/deliver`) is served by the hosted backend identically. **Provider-neutral provisioning** hooks are specified abstractly so the future Railway `cloud_persistent` and Daytona `cloud_sandbox` adapters plug in without further contract surface changes; target-scoped secrets are never returned to desktop/web clients. No closed DB vocabulary, no breaking interface change in this entry — `execution_targets.type` is already an open set and all new fields/endpoints/DTOs/profiles are additive; the runtime `CONTRACT_VERSION` constant (schema/migration baseline) is unchanged until the Postgres/runtime work lands its migrations. |
| `0.54-draft` | Adds an **admin-only workspace objective CSV export** on the workspace settings danger page (mission `coo:3`). **REST API Layer:** new additive endpoint `GET /api/workspaces/:id/objectives.csv` returns a `text/csv` attachment exporting every non-deleted objective in the requested workspace with exactly these columns: mission name, objective instructions, objective `created_at`, associated project name, and the mission's current workspace status name. Access is gated twice: the route requires `workspace:read`, and the handler must verify the caller is an `ADMIN` member of the **requested** workspace (not merely the currently active workspace). The export is read-only, server-generated, and does not persist artifacts, events, or files. **Web:** the workspace settings danger page gains an admin-only "Export objectives (.csv)" action that downloads this attachment for the selected workspace. No database schema, migration, protocol, auth vocabulary, connector, or branch-planning-vector impact. |
| `0.53-draft` | **Patch-level path note (mission `coo:24`):** the protocol/service core source tree moved from root `src/` to `packages/core/`, giving the shared service, repository helpers, and generated DB types a named package location without changing any interaction surface, command, DTO, schema, vocabulary, or branch-planning fixture. No database schema, migration, closed vocabulary, auth, connector capability, or branch-planning-vector impact. |
| `0.52-draft` | Adds **Everhour time-tracking integration** (mission `coo:29`). **Database Layer (additive, nullable — backward compatible):** new migration `20260625000000_missions_everhour_task` adds `missions.everhour_task_id` to both SQLite and Postgres (kysely types + schema-contract doc updated) — the Everhour task a mission is linked to (platform-prefixed string, e.g. `ev:3000010034`), written on first timer start / link. Existing rows default to null. The workspace Everhour **API key** is stored server-side in the existing `workspaces.settings_json` blob and is **never returned to the client**; the linked Everhour project id/name/section live in the existing `projects.settings_json` blob — no new columns for either. **REST API Layer:** all Everhour API calls are proxied server-side so the key never reaches the browser. New endpoints: `GET/PUT/DELETE /api/integrations/everhour` (workspace key presence/set/clear, gated `workspace:read`/`workspace:update`; `PUT` validates the key against Everhour `/users/me` before persisting); `PUT /api/projects/:id/everhour-link` (find-or-create + link an Everhour project by name, gated `project:update`); `GET /api/missions/:id/everhour` (gated `mission:read`) and the mutating `POST /api/missions/:id/everhour/timer/start`, `.../timer/stop`, `POST .../everhour/time`, `PATCH/DELETE .../everhour/time/:recordId` (gated `mission:update`). New additive DTOs: `EverhourIntegrationDto`, `UpdateEverhourIntegrationBody`, `LinkProjectEverhourBody`, `MissionEverhourStateDto`, `EverhourTimerDto`, `EverhourTimeRecordDto`, `CreateEverhourTimeBody`, `UpdateEverhourTimeBody`; `ProjectDto` gains read-only `everhourProjectName` / `everhourProjectId`. Durations are seconds, dates `YYYY-MM-DD`. **Web:** a Settings → Integrations page manages the API key; project settings adds an Everhour-project name field (defaulting to the project name); the mission panel header gets a time-tracking popover (start/stop timer, list/add/edit/delete time records) and the mission card hover footer gets a small play/stop timer circle — all Everhour UI is gated on the key being present. No closed DB vocabulary, protocol, auth, connector, or branch-planning-vector impact (runtime `CONTRACT_VERSION` constant, which tracks the schema/migration baseline, unchanged). |
| `0.51-draft` | Refines the objective launch/execution queue contract (mission `coo:23`). **Protocol Layer:** removes unsupported runner/device/project-resource queue commands from the stable `ovld protocol` command registry; agent protocol remains focused on mission lifecycle, context, updates, questions, delivery, hooks, and discovery. **Runner Layer / REST API Layer:** `/api/runner/status`, `/api/runner/claim`, `/api/runner/clear`, `/api/runner/requests/:id/launching`, `/api/runner/requests/:id/launched`, `/api/runner/requests/:id/failed`, and `POST /api/missions/:id/branch-prepared` are the sanctioned Runner -> REST queue/launch surface used by `ovld runner`. Runner queue transitions must be centralized in the service layer, record mission events and entity changes in the same transaction, set claim metadata (`claimed_by_device_id`, `claimed_by_execution_target_id`, `claim_expires_at`), reject illegal state jumps, and link attached agent sessions through `execution_requests.launched_session_id` when the launch context provides the request id. No database schema, migration, closed vocabulary, auth, connector capability, or branch-planning-vector impact. |
| `0.50-draft` | Changes unconfigured mission branch parent resolution from hardcoded `main` to the user's current primary checkout branch (mission `coo:14`). **Runner Layer:** `prepareMissionBranch` still honors a valid project-configured `MissionBranchDto.baseBranch` first, then resolves the base from the repository's main worktree checkout (ignoring a linked worktree the runner may be standing in), and only then falls back to the repo default. **REST API Layer:** pending `MissionBranchDto.baseBranch` uses the project default when set, otherwise the primary resource's main worktree checkout, otherwise `main`; prepared missions read the branch-prepared event payload's `baseBranch` so status derivation, branch actions, and worktree listings stay tied to the parent the runner actually used even if the user later checks out another branch. No REST shape, database schema, migration, closed vocabulary, protocol, auth, connector, or shared branch-planning-vector impact. |
| `0.49-draft` | Adds **primary resource creation from the web project's create dialog** (mission `coo:8`). **REST API Layer:** `CreateProjectBody` gains optional `primaryResource?: { directoryPath: string; executionTargetId?: string \| null }`. When present, `POST /api/projects` now creates the project and its initial primary `project_resource` atomically in the same server-side transaction, using the provided execution-target scope and writing `.overlord/project.json` for the linked directory the same way `POST /api/projects/:id/resources` already does. **Web:** the create-project modal now exposes a primary resource path field with the same desktop browse affordance used in project settings, and submits it through the new additive request body. No database schema, migration, closed vocabulary, protocol, auth, or connector impact. |
| `0.48-draft` | Adds a **per-mission worktree/branch opt-in** so users who keep the workspace-wide `worktreeBranchAutomationEnabled` setting off can still create a branch (and optionally a worktree) for selected missions (mission `coo:9`). **Database Layer (additive, nullable — backward compatible):** new migration `20260623120000_missions_worktree_preference` adds `missions.worktree_preference` to both SQLite and Postgres (kysely types + schema-contract doc updated). It is an app-validated open set (no DB CHECK): `null` inherits the workspace setting (the prior behavior), `'worktree'` forces a branch + worktree for the mission, `'branch'` forces a branch without a dedicated worktree (checked out in the project's primary repo). Unlike the one-shot `branch_override`, it is persistent and the runner does not clear it. **REST API Layer:** `MissionBranchDto` gains read-only `worktreeAutomationEnabled` (the workspace setting), `worktreePreference` (the per-mission value), and the resolved `willPrepareBranch` / `willUseWorktree` flags (derived in `webapp/server/repository.ts` by combining the two); `UpdateMissionBody` gains `worktreePreference?: 'worktree' \| 'branch' \| null` (validated; invalid values `400`). The branch-action/worktree-path resolution (`missionBranchDto` and `loadBranchActionContext`) now resolves a prepared branch's location via `git worktree list` (so a branch-only mission's commit/publish/dirty derivation operates in the primary repo), falling back to the canonical worktree path. New shared type `MissionWorktreePreference`. **Runner Layer:** `prepareMissionBranch` (`cli/src/branch-preparation.ts`) now resolves the effective decision from the mission DTO's `willPrepareBranch` / `willUseWorktree` (falling back to recomputing from `worktreePreference` + the workspace setting), preparing a dedicated worktree, a branch-only checkout in the primary repo, or nothing accordingly; the `--no-worktree` flag downgrades to branch-only and an explicit `--branch` still forces at least a branch. **Web:** the mission-header git control reads "<base>" when the mission runs off its base branch and its popover offers a create-branch affordance (canonical branch name + a "Create in a worktree" switch defaulting on) that sets `worktreePreference`; an opted-in-but-unprepared mission can revert to its base branch. The shared deterministic branch-planning algorithm and `contract/branch-planning-vectors.json` are unchanged. No closed DB vocabulary, protocol, auth, or connector impact (runtime `CONTRACT_VERSION` constant, which tracks the schema/migration baseline, unchanged). |
| `0.47-draft` | Adds a manual "Draft commit message with AI" action to the mission branch panel's commit affordance (mission `coo:4`), mirroring the `0.37-draft` "Generate title" pattern. The REST API Layer gains `POST /api/missions/:id/generate-commit-message` (gated `mission:update`; does not persist, so non-mutating), returning the new additive `GenerateCommitMessageResultDto { message: string }`. The handler resolves the mission's branch worktree (via the same `loadBranchActionContext` used by the branch actions), gathers the uncommitted work **read-only and host-side** (`git status --porcelain` + `git diff HEAD`, never staging or mutating the index), and drafts a concise commit message from it through the Automations Layer's existing Gemini surface (`generateGeminiText`, unchanged). Typed error `code`s: `BRANCH_NOTHING_TO_COMMIT` (the worktree is clean) and `COMMIT_MESSAGE_GENERATION_FAILED` (the summarizer is unavailable — e.g. no `GEMINI_API_KEY` — or returned nothing), reusing the existing `BRANCH_NOT_PREPARED` / `BRANCH_NO_PRIMARY` / `BRANCH_NO_WORKTREE` codes. The web mission panel's commit affordance is restyled: the single-line commit input becomes a three-row `textarea` (Cmd/Ctrl+Enter commits; bare Enter newlines) with a `Sparkles` icon button overlaid in its corner (loading/error states) that calls the new endpoint and drops the draft into the field for the user to edit before committing. Additive REST endpoint + DTO and client-only UI; no schema, migration, closed-vocabulary, protocol, auth, or connector impact (runtime `CONTRACT_VERSION` constant, which tracks the schema/migration baseline, unchanged). |
| `0.46-draft` | Breaking product-surface baseline rename to **missions** completed across stable component interfaces. **Database Layer:** existing SQLite and Postgres migrations are updated in place so the core work tables and foreign keys are `missions`, `mission_id`, `mission_events`, `mission_sequences`, `mission_tags`, `mission_tag_assignments`, and `my_mission_positions`; generated Kysely/logical types use mission names. **Protocol/CLI Layer:** protocol commands and management surfaces now use `--mission-id`, `ovld missions`, `search-missions`, mission session-key caching, and mission wording. **REST API Layer:** mission endpoints are rooted under `/api/missions` and DTO/type names use `Mission*`. **Runner/Connector/Web/Docs:** branch/worktree automation, connector workflow text, RBAC permissions, UI labels/components, and public docs now use mission terminology. This is a coordinated breaking rename with no compatibility aliases retained in the app surface; downstream integrations must update command flags, REST paths, database references, and permission names to mission terminology. |
| `0.45-draft` | Tightens the definition of a **merged** mission branch so it means "actually landed via the merge-with-parent flow," not merely "reachable from the base" (mission `coo:40`). Two coordinated fixes. **(1) REST API Layer** — `deriveBranchStatus` (`webapp/server/repository.ts`) now classifies a branch as `merged`/`merged_unpushed` only when its tip is contained in the base **and** is off the base's first-parent trunk (i.e. it entered through the `--no-ff` merge commit Action A creates, where the branch tip is the merge's second parent). A branch the base merely advanced past linearly — most commonly an empty mission branch whose parent moved forward via other merges — stays on the first-parent trunk and now reads `created`/`published` instead of a spurious `merged`. The status vocabulary (`pending \| created \| published \| merged_unpushed \| merged`) is unchanged; only the derivation is refined (extends the `0.39`/`0.40-draft` divergence rule with the first-parent-trunk check). **(2) Runner Layer** — `repoRefs` (`cli/src/branch-preparation.ts`) computes the `merged` set it feeds the shared `planMissionBranch` algorithm using the same first-parent-trunk rule, so a non-diverged established branch is no longer treated as merged. This stops the runner from cutting a fresh `-2`/`-3` cycle branch for every objective: objectives now reuse the mission's existing branch/worktree and only start a new cycle once the branch has genuinely been merged into its parent (the original intent of the per-mission planner). The shared deterministic planning algorithm and its conformance fixture (`contract/branch-planning-vectors.json`) are unchanged — this only corrects the `merged` input the Runner derives from live git state. Read-only derived-DTO derivation and internal Runner git-state logic; no database schema, migration, closed DB vocabulary, REST shape, protocol, auth, or connector impact (runtime `CONTRACT_VERSION` constant unchanged). |
| `0.44-draft` | Adds a **commit-before-merge** step to the on-demand branch-action surface (mission `coo:40`). (1) The read-only `MissionBranchDto` (derived in `webapp/server/repository.ts`) gains `dirty: boolean` — whether the branch's worktree has uncommitted changes (`git status --porcelain` non-empty in the worktree; `false` when no worktree exists or the branch is `pending`). (2) `BranchActionBody.action` widens from `integrate \| push_parent \| publish` to also include `commit`, and the body gains an optional `message?: string` (the commit message, required and non-empty when `action: 'commit'`). The new `commit` action stages and commits **all** changes in the branch's own worktree (`git add -A` then `git commit -m <message>`), running host-side like the other branch actions. Typed error `code`s added: `BRANCH_COMMIT_MESSAGE_REQUIRED` (empty/missing message, `400`), `BRANCH_NOTHING_TO_COMMIT` (the worktree is clean), and `BRANCH_COMMIT_FAILED` (the commit command failed); it reuses the existing `BRANCH_NO_WORKTREE` / `BRANCH_WORKTREE_MISMATCH` / `BRANCH_BUSY_EXECUTING` / `BRANCH_NOT_PREPARED` / `BRANCH_NO_PRIMARY` codes. The web mission panel uses `dirty` to gate the merge affordance: when the branch worktree is dirty it shows a "Commit changes" message prompt (calling `commit`) instead of "Update from <parent> & merge", which now appears only once the branch's changes are committed. Read-only derived-DTO field and an additive request-body action/field owned by the REST API Layer; no database schema, migration, closed DB vocabulary, protocol, auth, or connector impact (runtime `CONTRACT_VERSION` constant unchanged). |
| `0.43-draft` | Stops prefixing mission branch names with `overlord/` (mission `coo:39`). The Shared Deterministic Algorithm `canonicalMissionBranch` (in both `cli/src/branch-planning.ts` and `webapp/server/branch-planning.ts`) now renders `<mission-title-slug>-<mission-sequence>` instead of `overlord/<mission-title-slug>-<mission-sequence>`; the `BRANCH_PREFIX` constant is removed from both copies. The shared conformance fixture `contract/branch-planning-vectors.json` is regenerated to match (branch/worktree-path expectations drop the `overlord/`/`overlord-` segment); override branches and unrelated decision fields are unchanged. No schema, REST shape, closed-vocabulary, auth, or connector impact (runtime `CONTRACT_VERSION` constant unchanged). |
| `0.42-draft` | Branch selection, per-objective branch recording, and worktree lifecycle management (mission `coo:30`, objective 3). **Database Layer (additive, nullable columns — backward compatible):** new migration `20260623000000_objective_branch_and_override` adds `objectives.branch` (the branch an objective actually ran on, written by the runner at branch-prepared time) and `missions.branch_override` (a user-pinned branch overriding the planner default, cleared once consumed) to both SQLite and Postgres, with matching kysely types and schema-contract docs. Existing rows default to null; the schema-baseline runtime `CONTRACT_VERSION` constant is unchanged. **REST API Layer:** (1) `ObjectiveDto.branch: string \| null` surfaces the per-objective branch. (2) `MissionBranchDto.overrideBranch: string \| null` surfaces the pinned override; when set, the predicted branch name/worktree reflect it. (3) `UpdateMissionBody.branchOverride?: string \| null` sets/clears the override (empty/`null` clears). (4) New `GET /api/missions/:id/branches` → `MissionBranchListDto` `{ branches: string[]; current: string \| null }` lists the project primary repo's local+remote branches for the selector (gated `mission:read`). (5) New worktree-management surface (gated `project:read`/`project:update`): `GET /api/worktrees` → `WorktreeDto[]` (path, branch, project, mapped mission, derived status, `merged`, `dirty`, `sizeBytes`, `lastModifiedAt`); `POST /api/worktrees/remove` (`RemoveWorktreeBody` `{ path; force? }`, typed `WORKTREE_DIRTY`/`WORKTREE_NOT_FOUND`) and `POST /api/worktrees/purge-merged`, both returning `PurgeWorktreesResultDto` `{ removed; skipped; worktrees }`. **Runner/Service behavior:** the runner records `objectives.branch` and clears `missions.branch_override` at branch-prepared time; Action B (`push_parent`) now auto-removes the merged branch's clean worktree (auto-on-merge purge policy); the Runner Layer (`cli/src/branch-preparation.ts`) honors `MissionBranchDto.overrideBranch` at preparation (the explicit `--branch` flag still wins) and prunes stale worktree registrations before re-adding so follow-on objectives reuse the same worktree path. Like the `0.41` branch actions, worktree git ops run host-side in the REST API/service layer against `~/.ovld/worktrees`. No closed DB vocabulary, protocol, auth, or connector impact. |
| `0.41-draft` | Adds on-demand **branch action** mutations to the REST API Layer for the merge-with-parent flow (mission `coo:30`, Phase 1 of `planning/feature-plans/branching/branch-actions-pr-merge.md`). New endpoint `POST /api/missions/:id/branch/action` (gated `mission:update`) with body `BranchActionBody` `{ action: 'integrate' \| 'push_parent' \| 'publish'; confirmBusy?: boolean }`, returning the refreshed `MissionDetailDto`. `integrate` (Action A) merges the parent into the branch **inside the branch's own worktree** — the only step that can conflict; on conflict the merge is left in place and a typed `BRANCH_MERGE_CONFLICT` error names the worktree path so the user resolves it in their IDE — then advances the parent to the branch via a `--no-ff` merge commit. `push_parent` (Action B) pushes the merged parent to `origin`; `publish` pushes the branch itself (created → published). Typed error `code`s: `BRANCH_MERGE_CONFLICT`, `BRANCH_BUSY_EXECUTING` (an objective is executing on the branch; re-send with `confirmBusy: true`, re-checked server-side), `BRANCH_DIRTY`, `BRANCH_NO_WORKTREE`, `BRANCH_WORKTREE_MISMATCH`, `BRANCH_PARENT_NOT_CHECKED_OUT`, `BRANCH_PUSH_FAILED`, `BRANCH_NOT_PREPARED`, `BRANCH_NO_PRIMARY`. **Ownership note:** these git mutations run **host-side in the REST API/service layer** (`webapp/server/repository.ts`) directly against the project's worktrees under `~/.ovld/worktrees` and primary repo, which the webapp server is co-located with — extending the layer's existing host-side git use beyond the read-only `MissionBranchDto` derivation. The Runner Layer continues to own launching the *agent* into the worktree (it adopts the folder it is started in, e.g. an AgentPod); it does not own these on-demand branch mutations. No database schema, migration, closed DB vocabulary, protocol, auth, or connector impact (runtime `CONTRACT_VERSION` constant unchanged). |
| `0.40-draft` | Two REST API Layer additions for the "Configure branch merging and default branch" work (mission `coo:30`). (1) Adds the `merged_unpushed` value to the read-only `MissionBranchDto.status` vocabulary, which becomes `pending \| created \| published \| merged_unpushed \| merged` (derived in `webapp/server/repository.ts`). `merged_unpushed` means the branch's commits are contained in the **local** base but the base has not been pushed to `origin` yet — the intermediate state between merge Action A (advance the local parent) and Action B (push the parent). `deriveBranchStatus` now checks `origin/<base>` containment first (⇒ `merged`) and only-local-base containment second (⇒ `merged_unpushed`). The merge-with-parent flow advances the parent with a `--no-ff` merge commit precisely so parent and branch tips diverge, keeping this derivation unambiguous (a plain fast-forward leaves the tips identical and indistinguishable from a freshly-cut branch — the documented edge from `0.39-draft`). (2) Adds a project-configured base/parent branch: `ProjectDto.defaultBranch: string \| null` and `UpdateProjectBody.defaultBranch?: string \| null`, persisted under the `overlord.defaultBranch` key in the existing `projects.settings_json` blob (no new column/migration). It is the branch missions are cut from and the parent that "Merge with parent" advances; `null` falls back to `main`. The service layer (`repository.ts missionBranchDto`) now resolves the mission's `baseBranch` from this setting, and the Runner Layer (`cli/src/branch-preparation.ts`) prefers it (when the ref exists in the checkout) over the git-derived default when cutting branches. Read-only/derived-DTO and request-body additions owned by the REST API Layer; settings stored in an existing JSON column — no database schema, migration, closed DB vocabulary, protocol, auth, or connector impact (runtime `CONTRACT_VERSION` constant unchanged). |
| `0.39-draft` | Corrects and widens the read-only `MissionBranchDto.status` vocabulary surfaced by the REST API Layer (derived in `webapp/server/repository.ts` from `missions.active_branch`). The status set changes from `pending \| active \| merged` to `pending \| created \| published \| merged`. Previously a branch freshly cut from its base (tip identical to `main`, zero unique commits) was reported as `merged`, because merge detection used `git branch --merged <base>`, which lists any branch whose tip is reachable from the base — including a just-created branch. Merge detection now requires the branch to have **diverged** from its base (`branchSha !== baseSha`) and be fully contained in it (`rev-list --count <base>..<branch> === 0`), so a branch with no commits of its own never reads as merged; the prior "ref not found ⇒ merged" fallback is also corrected to `created`. The former catch-all `active` splits into `created` (branch created locally but not yet pushed to a remote) and `published` (a matching `refs/remotes/origin/<branch>` exists). Read-only derived-DTO vocabulary change owned by the REST API Layer; no database schema, migration, closed DB vocabulary, protocol, auth, or connector impact (runtime `CONTRACT_VERSION` constant, which tracks the schema/migration baseline, unchanged). |
| `0.38-draft` | Adds an optional per-repo `.overlordignore` file consumed by the CLI Layer's client-side changed-file capture (extends `0.12`/`0.16-draft`). Placed at the git repo root, it lists gitignore-style patterns (blank lines and `#` comments skipped; `!` negation, leading-`/` anchoring, trailing-`/` directory match, and `*`/`**`/`?` globs supported, last-match-wins) for files Overlord should never report as run-attributable changes (e.g. generated artifacts like `install-state.gz`). At `deliver`/`update` the CLI filters these paths out of the changed-file delta inside `filterRunAttributableChanges` (`cli/src/vcs.ts`), after the baseline-delta and touched-files intersection. VCS stays read on the client only; the backend persists what the client sends. Client/tooling-only and additive; no protocol command/flag, schema, REST shape, closed-vocabulary, auth, connector, or migration impact (runtime `CONTRACT_VERSION` constant unchanged). |
| `0.37-draft` | Adds a manual "Generate title" action to the mission title field. The REST API Layer gains `POST /api/missions/:id/generate-title` (gated `mission:update`), which synchronously invokes the existing Service → Automations surface (`generateObjectiveTitle`, unchanged) against the mission's earliest-position objective with non-empty instruction text — the same source used by the existing fire-and-forget creation-time title generation — persists the result, and returns the refreshed `MissionDetailDto`. Returns `400` when the mission has no objective with instruction text. The REST API Layer's internal `handle()` route wrapper now resolves the handler's return value through `Promise.resolve` so a handler may return either a plain value or a promise through the same response path; existing synchronous handlers are unaffected. The web mission panel's title field gains a `Sparkles` icon button (loading/success/error states, disabled with no objective text) that calls the new endpoint, mirroring the existing inline title editor's affordance. Additive REST endpoint and client-only UI; no schema, protocol, closed-vocabulary, or migration impact. |
| `0.36-draft` | Hardens the boundary between a containerized (agent-pod) CLI and the host's shared `overlord.toml`. The CLI Layer now **refuses to persist `overlord.toml` when running inside a container** — every config-writing path (`ovld config set`, `ovld setup`, `ovld init`, interactive backend config) funnels through `writeConfig` in `cli/src/config.ts`, which throws a `CliError` when `isRunningInContainer()` is true. This prevents an in-container process from baking a context-specific `backend_url` (e.g. `host.docker.internal:4310`) into a host-mounted file, where it would then leak onto every host process that resolves the same file. A containerized CLI is a pure consumer of the runtime `OVERLORD_BACKEND_URL` injected at pod launch (which already outranks the toml in `resolveBackendUrl`). Container detection (`isRunningInContainer` in `cli/src/env.ts`) prefers an explicit launcher-set `OVERLORD_IN_POD` marker (runtime-agnostic across Docker/podman/k8s) and falls back to the Docker `/.dockerenv` sentinel; `OVERLORD_ALLOW_CONFIG_WRITE=1` is a deliberate escape hatch (e.g. baking config into an image at build time). Pod launchers SHOULD set `OVERLORD_IN_POD=1` alongside the existing `OVERLORD_BACKEND_URL`/`OVERLORD_USER_TOKEN` injection. Client/tooling-only and additive; no schema, REST shape, closed-vocabulary, auth, connector, or migration impact (runtime `CONTRACT_VERSION` constant unchanged). |
| `0.35-draft` | Reverses the `0.34-draft` standalone-package extraction: the `open-overlord-branch-planner` workspace/npm package is removed. The deterministic per-mission branch/worktree planning helpers (`planMissionBranch`, `previewMissionBranch`, `missionWorktreePath`, `sanitizeBranchName`, `canonicalMissionBranch`, `slugifyBranchTitle`, and their `BranchDecision*` types) now live as plain internal modules **co-owned by the two components that consume them**: the Runner Layer (`cli/src/branch-planning.ts`, used by `cli/src/branch-preparation.ts`) and the service layer (`webapp/server/branch-planning.ts`, used by `webapp/server/repository.ts`). No component imports the other's copy across the boundary. Because the algorithm is now duplicated across that boundary, it is pinned to a single shared conformance fixture (`contract/branch-planning-vectors.json`): both the CLI and webapp test suites assert their implementation against the same golden input→output vectors, guaranteeing the runner's *prepared* branch/worktree always matches the webapp's *predicted* `MissionBranchDto` metadata. Any change to the planning algorithm MUST regenerate the fixture and bump the contract version. The published `open-overlord` CLI no longer depends on an external branch-planner package and the webapp drops the dependency too; the CLI publish flow no longer pre-builds it. Function signatures are unchanged; purely an internal packaging move with no schema, protocol, REST shape, closed-vocabulary, auth, connector, or migration impact (runtime `CONTRACT_VERSION` constant unchanged). See the new "Shared Deterministic Algorithms" section. |
| `0.34-draft` | Extracts the deterministic per-mission branch/worktree planning helpers (`planMissionBranch`, `previewMissionBranch`, `missionWorktreePath`, `sanitizeBranchName`, `canonicalMissionBranch`, `slugifyBranchTitle`, and their `BranchDecision*` types) out of the Automations Layer (`@overlord/automations`) into a new standalone, dependency-free package `open-overlord-branch-planner`, consumed by both the Runner Layer (CLI) and the service layer (webapp). This restores alignment with the Automations Layer's documented ownership (which never included branch/worktree planning) and lets the published `open-overlord` CLI depend on a public package rather than a private workspace. The Service → Automations surface no longer carries branch-preview helpers. Function signatures are unchanged; purely an internal packaging move with no schema, protocol, REST shape, closed-vocabulary, auth, connector, or migration impact (runtime `CONTRACT_VERSION` constant unchanged). Also fixes the CLI publish flow to use `yarn npm publish` (rewriting `workspace:` ranges to real semver) so installs no longer fail with `EUNSUPPORTEDPROTOCOL`. |
| `0.33-draft` | Adds mission worktree/branch automation behind a workspace setting. The Runner Layer prepares a per-mission branch and git worktree before local agent launch when `worktreeBranchAutomationEnabled` is true, records the resolved branch in the new `missions.active_branch` column (and a human-readable audit event under the existing `update` type) through `POST /api/missions/:id/branch-prepared`, and stamps the resolved branch/worktree into `execution_requests.launch_flags_json.branchAutomation`. Direct `ovld launch` accepts `--branch <name>` and `--no-worktree` escape hatches; runner launches stay branch-unaware after preparation and start in the prepared worktree. The REST API Layer adds derived, read-only `MissionDetailDto.branch` metadata (`name`, `baseBranch`, `worktreePath`, `status`) from `missions.active_branch`, and extends `/api/launch-settings` with `worktreeBranchAutomationEnabled` plus `PATCH /api/launch-settings/worktree-branch-automation`. The Database Layer adds the nullable `missions.active_branch` column via an additive forward migration on both the SQLite and Postgres adapters (kysely types regenerated), with a matching schema-contract update; the closed `mission_events.type` vocabulary is unchanged. Additive otherwise (DTO/settings); no protocol, auth, or connector impact. |
| `0.32-draft` | Resolves three recurring agent delivery-friction traps in the shared protocol envelope (see `planning/feature-plans/agent-delivery-friction.md`). (1) The CLI Layer caches the `attach` session key by `(resolve(workingDirectory), missionId)` and auto-injects `--session-key` on `update`/`heartbeat`/`ask`/`deliver`/`record-change-rationales` when the flag is absent, so protocol calls in fresh pod shells no longer fail the hard session-key requirement; an explicit `--session-key` always wins and the cache is cleared at `deliver`. (2) The CLI resolves each `--*-file` flag independently into a per-flag `fileInputs` map on the protocol request envelope (backend reads the per-flag entry, falling back to the legacy single `stdin` field), and fails fast with a `CliError` naming both flags when more than one `--*-file` uses literal `-` — replacing the previous silent collision where two stdin payloads routed to one field. (3) Change-rationale entries accept `filePath` (camelCase) as an alias for `file_path`, normalized to the canonical snake_case shape on `deliver`/`update`/`record-change-rationales`/`record-work`, so a rationale keyed by the changed-files casing no longer fails validation. Client/tooling and additive; `stdin`/`file_path` stay honored, no schema, REST shape, closed-vocabulary, or migration impact. |
| `0.31-draft` | Reclassifies `overlord.toml` and `.overlord/project.json` as per-instance/per-deployment artifacts that are **not committed** (gitignored like `.env.local`/`.env.prod`), generated by `ovld init`/`ovld setup`, with a committed `overlord.toml.example` template. Local development needs no `overlord.toml` and runs off `.env.local` plus code defaults. Reverses the prior "committed, host-context-neutral `overlord.toml`" guidance. Backend-URL resolution keeps the development (`OVERLORD_BACKEND_URL_DEV`, `.env.local`) and production (`OVERLORD_BACKEND_URL`, `.env.prod`) channels fully separate, and no tooling aliases the dev channel into the production variable (removed from `scripts/load-repo-env.mjs`, `webapp/load-repo-env.ts`, and `scripts/with-ovld-home.mjs`). Precedence is now explicit shell export > `overlord.toml` > backfilled env-file default, so an explicit `ovld config set`/`ovld init` (which writes the per-instance toml) takes effect over the `.env.local` default. A bare `ovld` invocation also now chooses its env profile by build origin (`detectCliEnvProfile`): an installed/published CLI under `node_modules` runs as `production` and never auto-loads `.env.local` or reads the dev-only `OVERLORD_BACKEND_URL_DEV`, even when run inside a dev checkout; only the source build / dev tooling defaults to `development`. This guarantees `OVERLORD_BACKEND_URL_DEV` cannot leak into a production CLI. Client/tooling-only and additive; no schema, REST shape, closed-vocabulary, or migration impact. |
| `0.30-draft` | Adds a read-side aggregate `completedObjectiveCount` to `MissionDto`: the count of non-deleted objectives in the `complete` state. Computed in the REST API Layer's `listMissions`/`searchMissions`/`getMissionRow` and `selectMyMissions` queries alongside the existing `objectiveCount`/`hasCompletedObjective` aggregates. The MissionCard board UI's bottom-right objective-count badge now displays this completed count (instead of total objectives), renders a subtler `muted` background outside the red alert state, and sits to the left of the assignee indicator. Additive; no schema, migration, or closed-vocabulary impact. |
| `0.29-draft` | Adds the **My Missions** selected-workspace view. The Database Layer gains the `my_mission_positions` table: personal (per-`workspace_user`), per-status-column drag ordering for the My Missions aggregate board, with a numeric `position`, `UNIQUE (workspace_id, workspace_user_id, mission_id)`, composite FKs `(workspace_id, mission_id) → missions` and `(workspace_id, status_id) → workspace_statuses`, and `ON DELETE CASCADE` (fires on hard delete only; soft-deleted missions/statuses are filtered at read time). The REST API Layer gains `GET /api/workspace/my-missions` → `MyMissionsResponse` of `MyMissionDto` (missions assigned to the active actor across the active workspace's projects, plus `projectName`/`projectColor`/`myPosition`; gated `mission:read`) and `PATCH /api/workspace/my-missions/order` (`MyMissionReorderRequest { statusId, orderedMissionIds }`; gated `mission:update`). Within-column reorders persist only `my_mission_positions` and never touch `missions.board_position`; a cross-column drag is a real status change that also writes `missions.status_id`/`status_type`/`board_position`, with the existing `(workspace_id, status_id)` composite FK as the integrity backstop, surfaced to the client as a typed `STATUS_UNAVAILABLE_FOR_WORKSPACE` error code. Folded into the `002_initial_core` baseline migration for both adapters (pre-release; existing local databases must be reset). Additive: one new table, two endpoints, and additive contract DTOs; no closed-vocabulary or breaking-interface change. |
| `0.28-draft` | The REST API Layer's `POST /api/projects/:id/resources` (`createProjectResource`) now writes/updates `.overlord/project.json` in the resource's directory, matching the CLI/service-layer `addProjectResource` behavior. Closes a gap where adding a resource via the web UI or `ovld add-cwd` (which calls this endpoint) left the resource directory without project association metadata. Additive behavior fix; no schema or REST shape impact. |
| `0.27-draft` | Adds `ovld prune [--json]` to the CLI Layer's DB-free command set. It deletes the contents of `.overlord/tmp` in the current directory (unlike the existing age-gated `pruneStaleProjectTmp` invoked at `ovld protocol` time, this removes everything regardless of age) and warns instead of deleting when no `.overlord` folder exists in the current directory. Client-only and additive; no schema, REST, or migration impact. |
| `0.26-draft` | Splits the CLI/backend target environment keys so production/global and in-repo development instances can coexist on one device. `OVERLORD_BACKEND_URL` remains the explicit runtime override and production/package env-file key. Development env files use `OVERLORD_BACKEND_URL_DEV`; repo dev wrappers map it into `OVERLORD_BACKEND_URL` only for their child processes so `yarn dev`, `yarn ovld:dev`, and related source workflows can target the dev API (for example `:4320`) without poisoning normal `ovld setup` / `ovld auth` resolution against the persisted `overlord.toml` production/Desktop target (for example `:4310`). CLI backend resolution is now: explicit runtime `OVERLORD_BACKEND_URL`, explicit runtime `OVERLORD_BACKEND_URL_DEV` in development profile only, persisted `overlord.toml`, profile env-file default (`OVERLORD_BACKEND_URL_DEV` from `.env.local` or `OVERLORD_BACKEND_URL` from `.env.prod`), then hardcoded fallback. Additive env-surface change; no schema, migration, or closed-vocabulary impact.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `0.25-draft` | Moves card statuses from project scope to workspace scope. The Database Layer renames `project_statuses` to `workspace_statuses`, drops its `project_id` column/foreign key, and re-keys uniqueness and the single-default/single-execute/single-review partial indexes on `workspace_id` instead of `project_id`; `missions.status_id` now references `workspace_statuses` (with an added `(workspace_id, status_id)` composite foreign key enforcing same-workspace status). Default statuses (`Backlog`/`In Progress`/`In Review`/`Done`) are seeded once per workspace — in the initial migration for the seeded `local-workspace` and by `createWorkspace` for new workspaces — instead of per-project at project creation. The REST API Layer replaces `GET/POST /api/projects/:id/statuses`, `PATCH /api/projects/:id/statuses/reorder`, and `PATCH/DELETE /api/projects/:id/statuses/:statusId` with workspace-scoped `GET/POST /api/workspace/statuses`, `PATCH /api/workspace/statuses/reorder`, and `PATCH/DELETE /api/workspace/statuses/:statusId` (gated on `workspace:read`/`workspace:update`). Contract DTOs `ProjectStatusDto`/`CreateProjectStatusBody`/`UpdateProjectStatusBody`/`ReorderProjectStatusesBody` are renamed to `WorkspaceStatusDto`/`CreateWorkspaceStatusBody`/`UpdateWorkspaceStatusBody`/`ReorderWorkspaceStatusesBody`; `WorkspaceStatusDto` exposes `workspaceId` instead of `projectId` and `MissionDetailDto.statuses` carries it. Moving a mission between projects no longer remaps its status (statuses are shared across the workspace). The web status-management UI moves from per-project settings (`WorkflowPage`) into workspace Settings. Breaking schema/interface change delivered by editing the initial migrations in place (no forward migration); reset required.                                                                                                                           |
| `0.24-draft` | Drops the `objectives.instruction_text NOT NULL` constraint (Database Layer) and removes the REST API Layer's `PATCH /api/objectives/:id` rejection of clearing instruction text to empty — `draft`/`future` objectives may now be edited down to blank instructions the same way they may be created blank (0.18-draft), instead of erroring. The web `DraftObjective` inline editor now commits an empty save for `draft` objectives (previously only `future`), so deleting all text in the card's instruction field leaves it empty rather than reverting to the prior value. Submitted and later states still require non-empty instruction text. The SQLite/Postgres full-text search triggers wrap `instruction_text` in `coalesce(..., '')` so a null value doesn't null out the generated search text. Schema/behavior relaxation only; no closed-vocabulary impact.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `0.23-draft` | Adds two read-side aggregate booleans to `MissionDto`: `hasCompletedObjective` (at least one non-deleted objective is `complete`) and `hasPendingObjectiveWithInstructions` (at least one non-deleted `draft`/`future` objective has non-empty instruction text). Powers the MissionCard board UI's bottom-right objective-count badge, which turns red when both are true (a completed objective exists alongside queued work that already has instructions). Computed in the REST API Layer's `listMissions`/`searchMissions`/`getMissionRow` queries alongside the existing `hasExecutingObjective` aggregate. Additive; no schema, migration, or closed-vocabulary impact.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `0.22-draft` | Adds `.env.local` development defaults and `.env.prod` production/package defaults beneath `overlord.toml` for the CLI/protocol backend target and local web/SQL Studio settings, and fixes env-var precedence so it can't invert the intended override order. Precedence, highest to lowest: (1) an explicit runtime env var set on the process before any env file is read (shell export, container/launcher injection — e.g. `OVERLORD_BACKEND_URL=http://host.docker.internal:4310` set when spawning a CLI process inside Docker), (2) the persisted `overlord.toml` value (user/Desktop-edited), (3) a profile-specific env-file baked-in default (`.env.local` for development, `.env.prod` for production/package builds), (4) the hardcoded fallback. Adds `cli/src/env.ts` (`loadEnvDefaults`, `isExplicitRuntimeEnv`), consumed by `resolveBackendUrl`/`resolveDatabasePath` in the CLI Layer and by the equivalent web-host/port/SQL-Studio resolution in the REST API Layer's server bootstrap. Clarifies that the **committed, shared** `overlord.toml` (e.g. a repo-root example file) must hold a host-context-neutral `backend_url` (the loopback default) — a context-specific value (a Docker-internal host alias, a remote URL) must be supplied as an explicit runtime env var at process launch, never edited into the shared file, since every process resolving that file inherits the same value regardless of its own network context. Additive; no schema or migration impact.                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `0.21-draft` | Removes the persistent seeded `local-user` identity from fresh local databases. Initial migrations may still create the placeholder local workspace and mission sequence, but the first human `user` / `profiles` / `workspace_users` rows must be created by the Auth Layer account creation flow. The auth-to-workspace bridge grants the first authenticated workspace member `ADMIN` when it creates that member row. Loopback protocol/runner fallback no longer fabricates `local-workspace-user` when no workspace user exists; unauthenticated local automation must wait until an account exists or authenticate with a user token. Delivered as a forward cleanup migration that deletes the untouched legacy seed identity only when it has no sessions, password/OAuth accounts, tokens, execution preferences, project preferences, projects, or missions.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `0.20-draft` | Hardens the `USER_TOKEN` model per the 2026-06-18 security audit and turns RBAC enforcement on across the backend. (1) Token creation now defaults `expires_at` to 90 days when no expiry is supplied; an explicit `null` expiry still means "never expires". (2) Implements the previously-reserved token scopes: `CreateUserTokenBody` accepts a `scope` (`full` \| `mission_lifecycle`), `UserTokenDto` returns the resolved `scope`/`scopeGrants`, and non-full scopes persist grant patterns into `user_token_scopes`. A token's effective permissions are its creating user's role grants **intersected with** its scope grants (absence of scope rows = full role grants); scopes can only restrict, never exceed, the user's role. The `mission_lifecycle` preset grants `project:read`, `mission:*`, `objective:*`, `session:*`, `event:create`, `event:read`, `artifact:*`, `attachment:*`, `execution_request:{create,read,claim}` and excludes project/user/role/connector admin and `user_token:self:*`. (3) Adds a unified RBAC gate (`requirePermission`) applied to every `/api` route — including `/api/protocol/*` and `/api/runner/*`, which now authenticate instead of bypassing auth — resolving an `Actor` for session auth, token auth (role ∩ scope), or a loopback-trusted local-operator fallback (no session/token from localhost keeps the existing single-trusted-user behavior). `entity_changes.actor_token_id` is now populated for token-authenticated mutations. (4) Adds the `ovld user-token` CLI command group (`create`/`list`/`revoke`/`rename`, with `--scope`, `--expires-in`, `--no-expiry`) to the CLI Layer. (5) Adds an `ovld doctor` warning when the credentials directory is nested inside a known cloud-sync root, and a shared CLI token-redaction utility. Additive to schema (reuses existing `user_token_scopes` / `actor_token_id`); no new tables, migrations, or closed-vocabulary changes. |
| `0.19-draft` | Makes `profiles.handle` mirror the Better Auth account username instead of being a freely editable handle. The auth→profiles migration bridge now copies the Better Auth `user.name` (the account username) into `profiles.handle` on user insert, and a new `AFTER UPDATE OF name ON "user"` trigger keeps `profiles.handle` (and `updated_at`/`revision`) in sync whenever the username changes. Because the handle is now bridge-managed, `PATCH /api/profile` no longer accepts a `handle` field (removed from `UpdateProfileBody`); display name, email, and avatar remain editable. The Auth Layer enables Better Auth `user.changeEmail` so the webapp Account settings page can change the account username (the username is the local-part of the synthetic `<username>@overlord.local` sign-in email) and password through the Auth surface. Additive trigger/behavior change delivered as a forward migration (`20260618000000_profile_username_mirror.sql`) that also backfills existing null handles; no closed-vocabulary or new-table impact.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `0.18-draft` | Allows blank `draft`/`future` objectives so the add-objective affordance can create an empty editable slot rendered directly as a `DraftObjective` card for inline authoring. The `objectives.instruction_text` column remains `NOT NULL` but no longer has a trimmed non-empty CHECK constraint; service/API validation still requires non-empty instruction text for `submitted` and later states, and `PATCH /api/objectives/:id` still rejects clearing instruction text to empty. Blank objectives get a default `New objective` title and skip async title generation until authored; the web launch button (`AgentLaunchButton`) refuses to queue an objective with no instruction. Existing initial migrations are updated in place for the draft schema; no new vocabulary impact.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `0.17-draft` | Adds project-scoped mission tags. Introduces two database tables: `project_tags` (per-project tag definitions: `label`, optional `color`, `active` flag, soft-delete + revision) and `mission_tags` (the `(mission_id, tag_id)` assignment join). The REST API Layer gains `GET/POST /api/projects/:id/tags` and `PATCH/DELETE /api/projects/:id/tags/:tagId` for definition management, `CreateMissionBody` accepts an optional `tagIds[]`, and `MissionDto` returns a populated `tags[]`. Additive: new tables and additive DTO fields only; no closed-vocabulary or breaking-interface change.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `0.16-draft` | Makes changed-file capture attribute files to the agent that actually edited them under concurrency. In addition to the attach-time VCS baseline (0.12), a connector that ships a `PostToolUse` edit hook writes a per-session "touched files" log (`<OVLD_HOME                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | ~/.ovld>/vcs-touched/<sha256(abspath(cwd)+NUL+MISSION_ID)>.json`) listing the exact paths it edited. At `deliver`, the client run-attributable set becomes the VCS working-tree delta **intersected with** the touched-files log, so files dirtied by other missions after this session attached (which have no baseline entry) are excluded. The CLI clears the log at `attach`/`resume-follow-up`(alongside the baseline). Connectors without an edit hook write no log and fall back to the 0.12 baseline-delta behavior unchanged. Adds the`editHook`connector capability and the`PostToolUse` hook type to the Claude connector. Client-only and additive; no schema, vocabulary, or migration impact. |
| `0.15-draft` | Moves reusable per-user execution-target launch preferences from workspace-scoped target/access rows into `user_execution_target_preferences`, keyed by profile, target type, and stable target fingerprint. `execution_targets` now owns target identity/connection metadata only; `workspace_user_execution_targets` owns workspace-member access only. `ovld setup` continues configuring the default terminal through the REST launch-settings surface, but persistence now writes `user_execution_target_preferences.terminal_profile_json`. Breaking database schema change; the reset schema no longer includes `agent_flags_json` / `terminal_profile_json` on `execution_targets` or `workspace_user_execution_targets`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `0.14-draft` | Adds the `OVERLORD_BACKEND_URL` environment override for the client CLI/protocol backend target, mirroring the existing `OVLD_HOME` / `OVERLORD_SQLITE_PATH` / `OVERLORD_WEB_PORT` / `OVERLORD_USER_TOKEN` overrides. It takes precedence over the resolved `overlord.toml` `backend_url`, letting an in-repo build target an isolated local instance (e.g. a dev backend on `:4320`) without editing a committed `overlord.toml`. Also clarifies that `OVLD_HOME` relocates the _entire_ per-user data dir — VCS baselines and native-session caches now resolve under it too, not just the SQLite database. Additive; no schema, vocabulary, or migration impact.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `0.13-draft` | Moves per-agent connector installation from `ovld setup <agent>` / `ovld setup all` to `ovld agent-setup <agent>` / `ovld agent-setup all`. `ovld setup` becomes the CLI-owned interactive first-run configuration flow for backend selection, agent connector setup, and `terminal_launcher` selection. Additive to config shape and REST/database boundaries; no schema or migration impact.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `0.12-draft` | Makes changed-file capture mechanical instead of agent-enumerated. The client CLI records a VCS baseline (`git status` paths) when a work session begins (`attach`/`resume-follow-up`) and, at `deliver`, sends the run-attributable delta (current changed paths minus baseline) as changed files — so agents no longer have to manually list what they changed. Adds the `--no-file-changes` flag to `deliver` as the explicit "this run changed no files" escape hatch that skips rationale-coverage enforcement. `deliver` now accepts `--changed-files-json` / `--changed-files-file`, and rationale-coverage validation is objective-scoped (aggregated across all sessions for the objective) per the change-tracking spec. VCS is read on the client only; the backend persists what the client sends. Additive: no schema or migration impact.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `0.11-draft` | Changes the published npm CLI to a client-only runtime. The CLI owns command UX, config, connector setup, and local runner/agent launching, but it no longer opens SQLite or imports database/service internals. Local mode points at a loopback backend URL (served by Desktop/local backend) and cloud mode points at a hosted backend URL; both use the REST/backend-client surface. SQLite/native dependencies move behind the local backend package.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `0.10-draft` | Adds first-run CLI backend onboarding: `ovld config set` can select a local SQLite backend or a hosted/cloud Postgres backend, and `ovld auth login` must ensure a backend is configured before continuing. Additive: no schema, vocabulary, or migration impact.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `0.9-draft`  | Adds the `desktop` component (**Desktop Shell**): an optional Electron wrapper around the local webapp that supervises the bundled REST/realtime server and reuses existing surfaces — it loads the SPA + `/api/*` over the loopback origin and spawns the CLI (`ovld serve`/`runner`/`launch`) as subprocesses, owning no product logic. Adds the `desktop-shell` conformance `componentType`; the `shellToRest` and `shellToCli` interaction surfaces; and the `ovld serve` management command (boot a fully-initialized local instance: create → migrate → serve), plus the documented packaged-mode app-data fallback for `overlord.toml`/SQLite resolution. Additive: no schema, vocabulary, or migration impact.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `0.8-draft`  | Adds post-delivery follow-up recovery: `UserPromptSubmit` hook events may record `user_follow_up` activity without a live session, and explicit protocol follow-up resume reopens a completed objective as `pending_delivery` with a new session for changed-file tracking and redelivery.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `0.7-draft`  | Adds the `custom-automation` extension point: downstream repos that track OpenOverlord upstream register their own automations via the `OVERLORD_AUTOMATIONS_MODULE` env var (loaded at server boot by `loadExternalAutomations()`), without editing the built-in automation registry. Additive; no schema or migration impact.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `0.6-draft`  | Moves the default SQLite database location from the repo (`database/.local/Overlord.sqlite`) to the per-user global directory (`~/.ovld/Overlord.sqlite`, overridable via `OVLD_HOME`); the `overlord.toml` `database_path` key overrides it per instance. Adds the admin `database_url` key for pointing Overlord at a hosted/cloud database, which feeds the shared `resolveAdapter()` selection point.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `0.5-draft`  | Removes the built-in read-only SQLite browser REST surface and replaces it with optional SQL Studio launch metadata configured by `overlord.toml`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `0.4-draft`  | Renames application identity table `users` to `profiles`; profiles are created from Better Auth `user` rows with matching IDs; `workspace_users` now references `profile_id` and no longer stores a display-name override; `user_images` references profiles only.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `0.3-draft`  | Renames the AI Tools Layer (`ai-tools`) to the Automations Layer (`automations`); renames `serviceToAiTools` to `serviceToAutomations`; renames `AiTool` interface to `Automation`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `0.2-draft`  | Adds `authToDatabase` interaction surface and Better Auth implementation tables; clarifies Better Auth uses the configured database adapter. Adds the AI Tools Layer (`ai-tools`) and `serviceToAiTools` interaction surface.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `0.1-draft`  | Initial component interaction contract.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |

---

## Component Registry

Each component owns a defined set of responsibilities. No component may directly access another's internals — use only the declared interaction surfaces.

### 1. Protocol Layer

**Stable identifier**: `protocol`
**Reference spec**: [`cli/docs/03-agent-protocol.md`](cli/docs/03-agent-protocol.md)

Owns:

- All `ovld protocol` subcommand names and their required/optional flags
- The `packages/core/` protocol/service core source package: shared service functions, repository helpers, and generated database types used by protocol, REST, CLI tests, and database harnesses
- Session lifecycle: `attach → (update|heartbeat)* → (ask|deliver)`
- Context assembly format returned by `attach`
- Delivery payload structure and validation rules
- Changed-file and change-rationale recording protocol
- Idempotency key scope naming for protocol operations (`protocol.*`)

Does NOT own:

- Connector installation (→ Connector Layer)
- Database schema (→ Database Layer)
- Authentication mechanism (→ Auth Layer)
- Runner queue/device/project-resource management commands (→ CLI / Runner / REST Layers)

### 2. Database Layer

**Stable identifier**: `database`
**Reference spec**: [`database/docs/09-database-schema-contract.md`](database/docs/09-database-schema-contract.md)

Owns:

- Table definitions, column types, indexes, foreign keys, CHECK constraints
- Controlled vocabularies (closed and open sets)
- Soft-delete and revision semantics
- Migration versioning via `schema_migrations`
- Extension table naming rules (`ext_<name>_`)
- Logical types and their adapter mappings

Does NOT own:

- Service-layer business logic (owned by the layer calling into the database)
- REST response shapes (→ REST API Layer)

### 3. CLI Layer

**Stable identifier**: `cli`
**Reference spec**: [`cli/docs/02-cli-first-product-surface.md`](cli/docs/02-cli-first-product-surface.md)

Owns:

- Management command names and argument shapes, including `ovld setup` (interactive first-run configuration), `ovld serve` (boot a fully-initialized local instance in local/backend packages), `ovld config set` (select local loopback backend URL or hosted/cloud backend URL), and `ovld auth login` (first-run backend onboarding before authentication)
- Project linking and discovery from working directory
- Configuration file locations and formats (`overlord.toml`, `.overlord/project.json`), resolved by walking up from the working directory with a documented packaged-mode fallback to the OS app-data directory (e.g. `~/Library/Application Support/Overlord/overlord.toml`), including backend URL settings (`backend_url` for the active REST/backend endpoint, `backend_mode` as `local` or `cloud`), optional legacy database settings consumed only by local/backend packages. Terminal launch settings live on `user_execution_target_preferences.terminal_profile_json` for the local execution target fingerprint (provisioned from the device fingerprint during `ovld setup`). Config resolution layers, highest precedence first: an explicit runtime export of the channel variable (shell / container launcher) set before any env file loads, then the resolved `overlord.toml` `backend_url` (per-instance, e.g. written by `ovld config set`), then the profile env-file default (`.env.local`/`.env.prod`, backfilled), then a hardcoded fallback — see `resolveBackendUrl` in `cli/src/config.ts`. Development and production read **separate** backend env vars so the two channels never collide: development resolves `OVERLORD_BACKEND_URL_DEV` (`.env.local`, e.g. a dev backend on `:4320`) and never reads or writes the production `OVERLORD_BACKEND_URL`; production resolves `OVERLORD_BACKEND_URL` (`.env.prod`). No tooling aliases the dev channel into the production variable. The dev-only `OVERLORD_BACKEND_URL_DEV` must never reach a production app: a bare `ovld` invocation chooses its profile by build origin — an **installed/published** CLI (under `node_modules`) runs as `production` and never auto-loads `.env.local` or reads `OVERLORD_BACKEND_URL_DEV`, even inside a dev checkout, while only the in-repo source build and the dev/test tooling that runs it (`yarn dev`, `with-ovld-home`) default to `development` (`detectCliEnvProfile` in `cli/src/env.ts`). The webapp server and desktop pass their own profile explicitly (source-vs-bundled). A backfilled env-file value is only a default: an explicit `overlord.toml` outranks it (so `ovld config set`/`ovld init` take effect), and only a deliberate shell export of the channel variable outranks the toml. `overlord.toml` and `.overlord/project.json` are per-instance/per-deployment artifacts that are **not committed** — they are gitignored like `.env.local`/`.env.prod`, generated for a deployment by `ovld init`/`ovld setup`, with `overlord.toml.example` as the committed template. Local development needs no `overlord.toml`: it runs off `.env.local` plus code defaults. Each `ovld` process resolves its own config independently from its own working directory/env. A CLI running inside a container/agent pod is a pure consumer of the injected runtime `OVERLORD_BACKEND_URL` and **must not persist config**: `writeConfig` refuses to write `overlord.toml` when `isRunningInContainer()` (an explicit launcher `OVERLORD_IN_POD` marker, or the Docker `/.dockerenv` sentinel) is true unless `OVERLORD_ALLOW_CONFIG_WRITE=1` overrides it — preventing a context-specific value from leaking through a host-mounted file onto every host process that resolves it.
- The optional per-repo `.overlordignore` file (git repo root): gitignore-style patterns for paths the client-side changed-file capture must never report as run-attributable changes (see the `0.37-draft` changelog entry and the changed-file capture notes in `0.12`/`0.16-draft`). Parsed and applied entirely on the client in `cli/src/vcs.ts`.
- Human-readable CLI output format conventions

Does NOT own:

- Protocol commands (→ Protocol Layer)
- Agent connector installation (→ Connector Layer)

### 4. Connector Layer

**Stable identifier**: `connector`
**Reference spec**: [`connectors/docs/05-connectors-and-agent-plugins.md`](connectors/docs/05-connectors-and-agent-plugins.md)

Owns:

- Connector core workflow instructions
- Per-agent plugin/adapter files and managed file manifests
- Hook scripts and their event contracts
- `ovld agent-setup <agent>` / `ovld agent-setup all` and `ovld doctor` behavior
- Connector capability declarations (the approved capability flag set)

Does NOT own:

- Protocol command implementations (→ Protocol Layer)
- Harness extension catalog (→ Extension System)

### 5. Runner Layer

**Stable identifier**: `runner`
**Reference spec**: [`cli/docs/04-runner-and-launch-execution.md`](cli/docs/04-runner-and-launch-execution.md)

Owns:

- `execution_requests` queue claiming and launch
- Working directory resolution
- Per-mission branch/worktree preparation before local agent spawn when enabled, including the deterministic branch/worktree **planning** algorithm (`cli/src/branch-planning.ts`) — co-owned with the service layer and pinned to `contract/branch-planning-vectors.json` (see "Shared Deterministic Algorithms")
- `ovld runner` commands
- Execution target selection logic
- Runner queue REST operations used by `ovld runner`: `GET /api/runner/status`, `POST /api/runner/claim`, `POST /api/runner/clear`, `POST /api/runner/requests/:id/launching`, `POST /api/runner/requests/:id/launched`, and `POST /api/runner/requests/:id/failed`
- Branch-preparation acknowledgement through `POST /api/missions/:id/branch-prepared`

Does NOT own:

- Protocol session lifecycle (→ Protocol Layer)
- Connector installation (→ Connector Layer)

### 6. REST API Layer

**Stable identifier**: `rest`
**Reference spec**: `database/docs/09-database-schema-contract.md` → REST API Boundary section

Owns:

- URL paths and HTTP method contracts
- Request/response DTO shapes (derived from the logical schema's camelCase field names)
- Read-only derived mission branch metadata (`MissionBranchDto`) from `missions.active_branch`, predicted via the service layer's copy of the shared branch/worktree planning algorithm (`webapp/server/branch-planning.ts`) — co-owned with the Runner Layer and pinned to `contract/branch-planning-vectors.json` (see "Shared Deterministic Algorithms")
- On-demand branch-action git mutations (`POST /api/missions/:id/branch/action`: commit / merge-with-parent / push-parent / publish) run host-side against the project's worktrees under `~/.ovld/worktrees` and primary repo (`webapp/server/repository.ts`). The Runner Layer owns launching the *agent* into a worktree, not these on-demand mutations.
- REST auth integration points
- SSE/WebSocket realtime endpoint
- SQL Studio launch metadata in `/api/meta` when the optional external SQL Studio process is enabled

Does NOT own:

- Database schema (→ Database Layer)
- Protocol CLI surface (→ Protocol Layer)

### 7. Auth Layer

**Stable identifier**: `auth`
**Reference specs**: [`auth/docs/07-user-token-authentication.md`](auth/docs/07-user-token-authentication.md), [`auth/docs/08-role-based-access-control.md`](auth/docs/08-role-based-access-control.md)

Owns:

- Token creation, rotation, revocation, and hash storage
- Role assignment CRUD
- Permission check interface
- Audit log attribution fields
- Better Auth implementation tables (`user`, `session`, `account`, `verification`, `apikey`) — these are auth-internal and must not be read directly by other components

Does NOT own:

- User identity schema (co-owned with Database Layer via schema contract)
- Application business logic gating (callers act on the result of a permission check)

### 8. Automations Layer

**Stable identifier**: `automations`
**Reference spec**: [`automations/docs/01-automations-overview.md`](automations/docs/01-automations-overview.md)

Owns:

- Pluggable automation interface (`Automation`) and built-in automation registry
- Provider configuration for optional Gemini-backed tools (`GEMINI_API_KEY`, model selection)
- Reference summarization tools (text summarization, objective title generation)
- Fire-and-forget automation helpers that callers invoke through injected persistence callbacks
- Downstream automation loading via `OVERLORD_AUTOMATIONS_MODULE` (the `custom-automation` extension point)

Does NOT own:

- Database schema (→ Database Layer)
- Mission/objective lifecycle state machines (→ CLI / service layer callers)
- Agent protocol or connector behavior (→ Protocol / Connector Layers)

### 9. Extension System

**Stable identifier**: `extension`
**Reference specs**: `database/docs/09-database-schema-contract.md` → Extension Points section; [`connectors/docs/agent-harness-configuration-architecture.md`](connectors/docs/agent-harness-configuration-architecture.md)

Owns:

- `user_harness_extensions` authoring workflow
- `workspace_harness_extensions` catalog promotion workflow
- Extension table namespace (`ext_<name>_`)
- Extension migration component naming (`ext:<name>`)
- Namespaced JSON metadata keys inside core tables

Does NOT own:

- Core table schemas (→ Database Layer)
- Built-in connector catalog (→ Connector Layer)

### 10. Desktop Shell

**Stable identifier**: `desktop`
**Reference spec**: [`desktop/docs/desktop-app.md`](desktop/docs/desktop-app.md)

An **optional** Electron wrapper around the webapp. It is a thin desktop shell, not a reimplementation of product logic: it loads the web control center in a hardened `BrowserWindow`, supervises the local processes the webapp depends on (Local mode), and gives the user a native app shell.

The shell runs in one of two modes, selected by the active **backend profile** (Local is the default and the unchanged behavior all prior contract text describes):

- **Local profile** (unchanged): fork the embedded web/REST server (`utilityProcess`) and load the bundled SPA from the loopback origin; API base = the loopback origin; Better Auth session cookies are same-origin; CSP/nav guard are scoped to that single loopback origin.
- **Remote profile** (additive, `0.55-draft`): **do not** fork the embedded server. The bundled SPA still loads from a local origin (privileged renderers never load remote content), but its runtime-injected **API base URL** points at the hosted backend. Auth uses a per-backend **bearer token** obtained at login and persisted via `safeStorage`/keychain, injected into the SPA's API client and into any spawned `ovld` process (not the local defaults). CSP `connect-src` and the navigation guard are recomputed from the active profile to allow the remote REST + `wss://` origins while keeping navigation shell-local; `/api/health` checks support HTTPS with a Local fallback.

Owns:

- The Electron app/window lifecycle and the hardened `BrowserWindow` security baseline (`contextIsolation`, `sandbox`, `nodeIntegration: false`, `preload`), the loopback-scoped CSP, single-instance lock, external-navigation handling, native window chrome (macOS `hiddenInset` title bar + inset traffic-light position so the webapp nav serves as the title bar), and the sandboxed renderer's native context menu (spellcheck/edit roles)
- Process supervision of the bundled web/REST server (forked as a Node `utilityProcess` in **Local** mode only; **not forked** in Remote mode) and, optionally, a local runner
- Persisted backend **profiles** (`{ id, label, mode: 'local' | 'remote', backendUrl }` + active profile id) in `userData/settings.json`, per-profile Electron **session partitions** (`persist:backend-<id>`), per-backend bearer-token storage via `safeStorage`, and the reload-and-login switcher / first-run chooser
- The minimal `preload` bridge surface exposed to the renderer as `window.overlord` (feature-detected by the SPA; never required by it), including the additive backend-config ops `getActiveBackend()`, `listBackends()`, `addBackend()`, `removeBackend()`, and `switchBackend(id)`, plus `writeProjectMetadata(...)` for local checkout metadata writes in remote-client mode and `invokeLocalTarget(call)` for unified checkout-local capabilities (`0.61-draft`)
- Desktop packaging metadata and the build/sign/notarize pipeline (`electron-builder`, entitlements, app-data layout)

Does NOT own:

- REST/SSE URL paths or DTO shapes (→ REST API Layer)
- CLI/launch/terminal configuration, including per-user terminal profiles on `user_execution_target_preferences` (→ CLI / Runner Layers)
- Authentication mechanism (→ Auth Layer)
- Database schema or adapter selection (→ Database Layer)

Depends on:

- `rest` — loads the SPA and calls `/api/*` over the loopback origin
- `cli` / `runner` — spawns `ovld serve` / `ovld runner` / `ovld launch` as subprocesses
- `auth` — in-app auth uses Better Auth session cookies on the same loopback origin; spawned CLI uses `USER_TOKEN` when a credential is required

The dependency arrow points one way only: `webapp`, `cli`, and `database` MUST NOT depend on `desktop`.

---

## Interaction Surfaces

These are the **only sanctioned paths** between components. Bypassing these surfaces is a contract violation.

### Agent → Protocol (Primary Mission Surface)

- **Transport**: Subprocess CLI invocation of `ovld protocol <command>`
- **Authentication**: Session key passed as flag or environment variable
- **Required sequence**: `attach → (update|heartbeat)* → (ask|deliver)`
- **Response format**: JSON on stdout; non-zero exit on error
- **Shell-special content**: Must use `--summary-file -` / `--payload-file -` with stdin heredoc

### Protocol → Database (Service Layer)

- **Transport**: Service layer function calls within ACID transactions
- **Rule**: No direct table writes from protocol handlers; use the service layer
- **Concurrency**: Optimistic compare-and-set via `revision`; zero-row update = `409` conflict
- **Change feed**: `entity_changes` appended in same transaction as domain mutation
- **Idempotency**: Protocol-scope idempotency keys in `idempotency_keys`

### CLI → REST (Backend Client Surface)

- **Transport**: HTTP/JSON to the configured backend URL (`backend_url`) over loopback for local mode or HTTPS for cloud mode
- **Rule**: The published npm CLI must not open SQLite, import database adapters, or write tables directly; mutations go through REST/backend endpoints that use the service layer/database runtime behind the backend process
- **Project context**: Resolved via backend APIs plus `.overlord/project.json` and working-directory hints sent in request payloads
- **Runner**: The CLI runner remains local and launches agents, but queue claim/status/mark-success/mark-failure operations go through backend APIs

### Connector → Protocol (Hook Surface)

- **Hooks**: `UserPromptSubmit`, `PermissionRequest`, `PostToolUse`, `Stop` (future)
- **Transport**: Shell scripts invoking `ovld protocol hook-event` or `update`
- **Rule**: Hook scripts must not write to the database directly; use protocol commands only
- **Edit capture**: a connector's `PostToolUse` edit hook records the files the agent edits into the per-session touched-files log read by the client at `deliver` to make changed-file attribution exact under concurrency (see the `0.16-draft` change-tracking notes). The hook writes only normalized absolute paths — never diffs or file contents — and must not write to the database directly
- **Follow-up capture**: `UserPromptSubmit` records `user_follow_up` activity even when the original delivery ended the session; it must not reopen implementation work by itself

### Runner → REST (Queue Surface)

- **Transport**: HTTP/JSON to the configured backend URL
- **Endpoints**: `GET /api/runner/status`, `POST /api/runner/claim`, `POST /api/runner/clear`, `POST /api/runner/requests/:id/launching`, `POST /api/runner/requests/:id/launched`, `POST /api/runner/requests/:id/failed`, and `POST /api/missions/:id/branch-prepared`
- **Claiming**: Backend performs an atomic compare-and-set claim on `execution_requests`; verifies launchable state
- **Side effects**: Backend claim and state transitions append `mission_events` and `entity_changes` in the same transaction
- **Claim metadata**: Claim must set `claimed_by_device_id`, `claimed_by_execution_target_id` when known, and `claim_expires_at`
- **State transitions**: `queued → claimed → launching → launched|failed`; active requests may be cleared; stale claims and launched-without-attach requests may move to `expired`; other terminal statuses are sinks unless a new request is explicitly queued
- **Attach correlation**: Protocol attach links the launched request to `agent_sessions` via `execution_requests.launched_session_id` when the launched context supplies an execution request id

### REST API → Database (HTTP Surface)

- **Transport**: Service layer function calls; same service layer as CLI and Protocol
- **Auth**: Token or session auth via Auth Layer before service call
- **Response shape**: Derived from logical schema camelCase field names
- **Idempotency**: REST-scope idempotency keys for retried writes

### Auth → Database (Identity Bridge)

- **Transport**: Better Auth's configured database adapter for auth tables; direct service-layer/database adapter queries for identity bridging
- **Auth tables owned**: `user`, `session`, `account`, `verification`, `apikey` — auth-internal; no other component accesses them directly
- **Identity bridge**: Auth Layer reads `workspace_users` and `profiles` (via `profiles.id = Better Auth user.id`) to resolve an authenticated identity to an Overlord `Actor`
- **Role resolution**: Auth Layer reads `role_assignments` for the resolved workspace user to build the `Actor`'s role list
- **Rule**: Auth Layer must not write to core domain tables (missions, projects, etc.)

### Service → Automations (Automation Surface)

- **Transport**: Service-layer function calls to the automations module's exported package API (`@overlord/automations`)
- **Configuration**: Environment variables documented in `.env.prod.example` / `.env.local.example` (`GEMINI_API_KEY`, optional model override)
- **Rule**: Automations must not read or write domain tables directly; persistence goes through caller-supplied store interfaces (e.g. `ObjectiveTitleStore`)
- **Fallback**: When a provider is unavailable or a call fails, automations return `null` and callers use deterministic local fallbacks
- **Downstream extension**: Forks add automations via `OVERLORD_AUTOMATIONS_MODULE` (`custom-automation` extension point), registering through the module API — never by editing the built-in registry

### Extension → Core (Extension Surface)

- **Database**: Migrations in `ext_<name>_` prefixed tables; `schema_migrations.component = 'ext:<name>'`
- **Metadata**: Namespaced keys in `metadata_json` / `settings_json` (reverse-DNS or package-style)
- **Reactions**: Via service APIs, `entity_changes`, and `outbox_messages`; no direct writes to core tables
- **Connector extensions**: Via `user_harness_extensions` → `workspace_harness_extensions` promotion

### Desktop Shell → REST (Renderer Surface)

- **Transport**: The hardened `BrowserWindow` loads the SPA and calls `/api/*` (including the SSE realtime stream) — over the loopback `http`/`ws` origin in **Local** mode, or over the hosted backend's HTTPS/`wss` origin (a separate origin from the local shell) in **Remote** mode, via the SPA's runtime-injected API base URL. Either way the SPA is just another HTTP client of the existing REST surface
- **Auth**: In **Local** mode, Better Auth session cookies work natively because the renderer and API share the same loopback origin; the shell injects no headers and stores no tokens. In **Remote** mode (cross-origin), the shell prefers a **bearer token** over cross-site cookies: it is obtained at login, persisted per-backend via `safeStorage`, and injected into the SPA's API client — unifying with the CLI `USER_TOKEN` model
- **Rule**: The shell must not fork or modify the SPA; desktop-only affordances are exposed only through the feature-detected `window.overlord` preload bridge. Navigation stays scoped to the shell origin in both modes

### Desktop Shell → CLI (Process Supervision Surface)

- **Transport**: Subprocess / Node `utilityProcess` invocation of the bundled CLI and server (`ovld serve`, `ovld runner`, `ovld launch`), analogous to the Agent → Protocol subprocess pattern
- **Credential**: For spawned CLI that needs a network credential (remote/shared deployments), the shell passes a `USER_TOKEN` via `Overlord_USER_TOKEN`; pure-loopback execution talks to the local service layer and needs none
- **Rule**: The shell triggers existing CLI/runner behavior; it must not reimplement launch, terminal, or config logic the CLI/Runner Layers own

---

## Conformance Requirements

Any component, connector, or extension that ships against Overlord must:

1. **Provide a conformance manifest** (`conformance-manifest.yaml`) at its root declaring:
   - `contractVersion`: the version this component was validated against
   - `componentType`: one of `connector`, `extension`, `database-adapter`, `auth-provider`, `rest-module`, `desktop-shell`
   - `componentKey`: stable lowercase identifier
   - All capabilities and extension points it uses

2. **Pass validation** via `ovld contract check <manifest-file>`
   (or the equivalent validation script until `ovld contract` is implemented)

3. **Respect ownership boundaries**: only interact with other components through declared surfaces

4. **Use namespaced identifiers** for all extension values — table names, JSON keys, event types, vocabulary extensions

5. **Declare all vocabulary extensions** from open vocabularies in its conformance manifest

See [`contract/conformance-manifest.schema.yaml`](contract/conformance-manifest.schema.yaml) for the required manifest shape.

---

## Shared Deterministic Algorithms

A few pure, deterministic computations must produce **identical** results in more than one component. Rather than share a runtime package (which couples the components' build/publish graphs), each component keeps its own copy of the algorithm and both copies are pinned to a single committed **conformance fixture** of golden input→output vectors. CI runs both component test suites against the same fixture, so the implementations cannot drift apart.

### Per-mission branch/worktree planning

- **Algorithm**: `planMissionBranch`, `previewMissionBranch`, `missionWorktreePath`, `sanitizeBranchName`, `canonicalMissionBranch`, `slugifyBranchTitle` (and the `BranchDecision*` types).
- **Implementations** (must stay byte-for-byte equivalent in behavior):
  - Runner Layer — `cli/src/branch-planning.ts`, used by `cli/src/branch-preparation.ts` to **prepare** the real branch/worktree before launch.
  - Service layer (webapp) — `webapp/server/branch-planning.ts`, used by `webapp/server/repository.ts` to **predict** `MissionBranchDto` branch metadata surfaced over REST.
- **Conformance fixture**: [`contract/branch-planning-vectors.json`](contract/branch-planning-vectors.json). Both `cli/test/branch-planning.test.ts` and `webapp/server/branch-planning.test.ts` assert their implementation against every vector.
- **Rule**: Neither component may import the other's copy across the boundary. Any change to the algorithm MUST update both copies, regenerate the fixture, and **bump the contract version** — the fixture is a stable interface.

## Extension Points

The only sanctioned ways to extend Overlord:

| Extension Point        | Mechanism                                                                | Boundary                |
| ---------------------- | ------------------------------------------------------------------------ | ----------------------- |
| Custom agent connector | New adapter + `conformance-manifest.yaml`                                | Connector Layer         |
| Custom harness         | `user_harness_extensions` + workspace promotion                          | Extension System        |
| Custom automation      | Module(s) via `OVERLORD_AUTOMATIONS_MODULE` calling `registerAutomation` | Automations Layer       |
| Database adapter       | Implement full logical schema + pass conformance tests                   | Database Layer          |
| Database extension     | `ext_<name>_` tables + `schema_migrations.component`                     | Database Layer          |
| Auth/RBAC provider     | Auth Layer service boundary                                              | Auth Layer              |
| REST extension         | Namespaced endpoint prefix (`/ext/<name>/`)                              | REST API Layer          |
| Open vocabulary values | Namespaced values declared in manifest                                   | Database/Protocol Layer |

Attempting to extend Overlord through any other path — patching core tables directly, adding undeclared hook types, using closed vocabulary values — is a contract violation that must be resolved before the component ships.

See [`contract/extension-points.yaml`](contract/extension-points.yaml) for machine-readable declarations and the approved capability flag list.

---

## Controlled Vocabularies

### Closed (contract-version-bump required to add values)

- `objectives.state`: `future`, `draft`, `submitted`, `launching`, `executing`, `pending_delivery`, `complete`
- `workspace_statuses.type`: `draft`, `execute`, `review`, `complete`, `blocked`, `cancelled`
- `execution_requests.status`: `queued`, `claimed`, `launching`, `launched`, `failed`, `cleared`, `cancelled`, `expired`
- `agent_sessions.delivery_state`: `not_delivered`, `delivered`, `pending_redelivery`
- `mission_events.type`: `update`, `user_follow_up`, `alert`, `discussion_summary`, `decision`, `ask`, `permission_request`, `delivery`, `execution_requested`, `awaiting_approval`, `status_change`
- `permission_requests.status`: `requested`, `approved`, `denied`, `expired`, `not_required`
- `idempotency_keys.status`: `in_progress`, `completed`, `failed`
- `audit_log.result`: `allowed`, `denied`, `failed`

### Open (extensions may add namespaced values)

- `workspaces.kind`, `profiles.kind`
- `execution_targets.type`, `project_resources.type`
- `artifacts.type`
- `mission_events.source`, `entity_changes.entity_type`, `entity_changes.source`
- `outbox_messages.topic`, `worker_jobs.type`
- RBAC permission names
- Connector/agent identifiers

Full value lists live in the database schema contract "Controlled Vocabularies" section.

Post-delivery follow-up execution reuses existing vocabulary values: discussion-only
follow-ups append `mission_events.type = user_follow_up` while the objective remains
`complete`; explicit resume transitions the objective to `pending_delivery` and
creates a new session with `agent_sessions.delivery_state = pending_redelivery`.

---

## Machine-Readable Contract Files

The `contract/` directory contains machine-readable counterparts:

- [`contract/components.yaml`](contract/components.yaml) — Component registry with capabilities and interface declarations
- [`contract/protocol-commands.yaml`](contract/protocol-commands.yaml) — Protocol command names, required flags, and response shape versions
- [`contract/extension-points.yaml`](contract/extension-points.yaml) — Sanctioned extension points and approved capability flags
- [`contract/conformance-manifest.schema.yaml`](contract/conformance-manifest.schema.yaml) — JSON Schema (YAML) for conformance manifests
- [`contract/branch-planning-vectors.json`](contract/branch-planning-vectors.json) — Golden input→output vectors pinning the duplicated per-mission branch/worktree planning algorithm (see "Shared Deterministic Algorithms")

---

## Contract Maintenance Rules

### When to update this document

| Change                                 | Contract update required?                                                        |
| -------------------------------------- | -------------------------------------------------------------------------------- |
| New component added                    | Yes — add to component registry and `contract/components.yaml`                   |
| New interaction surface                | Yes — add to interaction surfaces and `contract/components.yaml`                 |
| Protocol command added/renamed         | Yes — update `contract/protocol-commands.yaml`                                   |
| Protocol command flag changed          | Yes — update `contract/protocol-commands.yaml`; bump version if breaking         |
| Database table/column added            | Yes — update `09-database-schema-contract.md`                                    |
| Closed vocabulary value added          | Yes — requires contract version bump                                             |
| Open vocabulary value promoted to core | Yes — add to `09-database-schema-contract.md` Controlled Vocabularies            |
| New extension point                    | Yes — add to `contract/extension-points.yaml` and this document                  |
| New connector capability flag          | Yes — add to `approvedConnectorCapabilities` in `contract/extension-points.yaml` |
| New connector shipped                  | Conformance manifest only; no contract update unless new capabilities needed     |
| Extension shipped                      | Conformance manifest only; no contract update unless new extension points needed |

### Procedure for contract-modifying changes

1. Read and understand the current contract (this document + relevant `contract/*.yaml` files)
2. Draft the required contract changes
3. **Increment the contract version** in this document header and in `contract/components.yaml`
4. Add a changelog entry to this document
5. Implement the changes in code and other docs
6. Verify any affected component's conformance manifest passes `ovld contract check`

**The implementation code must not land before the contract update.** If you are implementing code that would conflict with or extend the existing contract without updating it first, stop and update the contract first.
