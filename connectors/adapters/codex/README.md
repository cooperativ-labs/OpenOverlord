# Overlord — Codex plugin

Codex plugin that exposes the Overlord local ticket workflow to Codex chat sessions.

## Overlord layer map

This connector is intentionally reviewable against the four connector layers in `connectors/README.md`:

- Connector Core: `../../core/overlord-ticket/SKILL.md` and its `reference/` files carry the durable attach, update, ask, and deliver workflow.
- Connector Plugins: `skills/overlord-ticket/SKILL.md`, `.codex-plugin/hooks.json`, and `scripts/` extend the core for Codex.
- Plugin Adapters: `.codex-plugin/plugin.json` declares the Codex-native plugin package and install-time config.
- Prompt Wrappers: `prompt-wrapper.md` describes the launch-time prompt shape for Codex.

## What ships

- `skills/overlord-ticket/SKILL.md` — Codex-specific overlay bundled with the shared core reference files.
- `.codex-plugin/hooks.json` + `scripts/*.sh` — `UserPromptSubmit` follow-up capture and `PermissionRequest` notifications through `ovld protocol`.
- `.mcp.json` + `scripts/overlord-mcp.mjs` — MCP bridge to common `ovld protocol` operations.
- `assets/` — branded plugin assets for Codex install surfaces.
- `conformance-manifest.yaml` — connector conformance declaration for the Overlord contract.
- `prompt-wrapper.md` — Codex launch guidance.

The shared Connector Core source is outside this adapter at `connectors/core/overlord-ticket`. Setup copies the bundled skill and reference files into the installed plugin so the runtime package is self-contained.

## Requirements

- Overlord CLI (`ovld`) on `PATH`. See the Overlord docs / Settings → Agents & MCP.
- Codex CLI (`codex`) on `PATH` for Overlord-launched sessions and plugin enablement.
- `TICKET_ID` is exported automatically when a Codex session is launched from Overlord.

## Install

From an OpenOverlord checkout:

```bash
ovld agent-setup codex
```

This copies managed plugin files to `~/.codex/plugins/overlord`, merges protocol permission rules into `~/.codex/rules/default.rules`, writes the personal marketplace entry at `~/.agents/plugins/marketplace.json`, and runs `codex plugin add overlord@overlord-local` when the Codex CLI is available.

Re-run safely any time the connector contract version changes:

```bash
ovld agent-setup codex
ovld doctor
```

## Permission warmup

Codex may still prompt once for `ovld protocol` approval on first use. Run this inside a Codex session and approve the prefix when prompted:

```bash
ovld protocol help
```

## Namespaced components

Inside Codex the plugin surfaces as:

- skill → `overlord-ticket`
- MCP server → `overlord`

Prompts generated by the runner should reference these names in bundle instruction mode.
