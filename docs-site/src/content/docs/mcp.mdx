---
title: MCP access
description: Let supported assistants work with Overlord through MCP.
---

MCP access lets a supported assistant read mission context and use approved Overlord actions from its own interface. The assistant still follows workspace permissions and the mission lifecycle.

Use the connector setup flow for your assistant, then confirm access with `ovld doctor`. Do not paste user tokens into prompts or source files.
